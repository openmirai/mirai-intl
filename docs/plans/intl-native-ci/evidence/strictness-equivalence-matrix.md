# Strictness and equivalence approval matrix

Source reviewed 2026-09-10 at library HEAD `7f02a9ec5c555bfd993395e1c1573ff7902f9240`, with current working-tree test bodies. This document is the only file changed by this update. The original read-only audit ran no tests; the subsequent authorized packed-smoke execution below is now recorded separately. No tests were rerun for this documentation update. References below are repository-relative `path:line`; `test/` in the matrix means `packages/compiler/test/`, and `src/` means `packages/compiler/src/`.

## Evidence ledger — do not combine these into one acceptance claim

| ID | Evidence | What it establishes / limitation |
| --- | --- | --- |
| N | Main reports native CI **34459228336**, full forced-Rust `pnpm verify`, green. | Library CI integration gates passed at that run’s revision. Status supplied by main, not freshly fetched in this audit. Some unit tests deliberately mock native success/failure; a forced-Rust environment does not turn those into real-addon tests. |
| C | Main reports normal CI **34459228084**, green. | Normal library CI passed. Do not infer that every native transport test ran: `vitest.config.ts:41` includes TypeScript tests under packages/test, not `native/test/*.test.mjs`. |
| P | `current-production-builds.json` in this directory; four hashed raw logs. | Four local production builds passed on Node 24.18.0/macOS arm64, forced Rust, CI all-eight assets, no deployment. One sample per app; finalized proof files were replaced by later reuse benchmarking. The hashed logs retain finalizer outcomes/counters; they are not archived finalized proof objects. |
| S | `shared-workspace-parser-proof.json` in this directory. | Actual packed workspace `check --workspace` instrumentation, five catalogs; 3,931 shared keys / 7,862 locale entries / 5,718 distinct raw strings. Each shared string parsed once in coordinator, zero times in every child. Child app-specific parses remain (admin 6,996; learner 3,520; instructor 3,469; auth 103; shared package 0). This proves that observed corpus and exact recorded compiler/parser bytes, not arbitrary future catalogs or all semantic-stage hit counts. |
| T1 | Main reports Turbo CI **34462283837** hit, producer-state isolation, import success; **typecheck failed**. | Positive authority-transfer/reuse lane evidence only. Not a green Turbo adoption run or four CI production builds. |
| T2 | Main subsequently reports Turbo CI **34464044260** failed typecheck again. | Superseded by T3; not a green adoption result. |
| T3 | Main reports Turbo head **5fa660**, candidate **34468546926** source quality passed and tests running; ordinary **34468547449** quality succeeded. | Status supplied by main, not freshly fetched here. Candidate tests/build/final proof acceptance remain pending; ordinary quality success is not full candidate success. |
| K | Executed forced-Rust packed smoke, **exit 0**, Node 24.18.0, current `.29` all-eight assets. | Seven isolated locale negatives, all eleven existing receipt negatives, valid translation/contract-change generation and fresh authorization, EN/TH installed runtime output, baseline restoration, unchanged rerun, and packed NodeNext typecheck passed. Exact source/evidence bindings below. |

P records consumer revision `42a898e58b7355326d3da121f56b63f1c995a161`, compiler source `f71bbc868615cce0d877f6c9e456421cd9cd1058`, and pack SHA256 `8854d068b1b692ffe5b32dff5bf5d06ab75712ac67f1c2ccf22ccfce899da1cd`. Thus P is not silently attributed to the current L HEAD. Auth/instructor/admin/learner logs report respectively 133/941/1,373/1,062 modules, two targets each, one receipt verification each, zero semantic builds. Exact raw log paths and SHA256s are preserved in that JSON.

## Executed packed-smoke binding (K)

This is **uncommitted source evidence**, not a run of pristine HEAD or a published `.30` package: base HEAD `7f02a9ec5c555bfd993395e1c1573ff7902f9240` plus the working-tree `scripts/pack-smoke.ts` diff. At documentation handoff `git status --short -- scripts/pack-smoke.ts` was ` M scripts/pack-smoke.ts`. The run packed existing library dist/assets; packed-byte hashes, rather than HEAD alone, identify the tested binaries. No version bump, commit, tag, push or publication was performed.

| Bound artifact | SHA256 |
| --- | --- |
| `/private/tmp/intl-pack-smoke-final-green.log` (successful stdout/run log) | `6fb7bb948edb5e092130c9698e437dabed82589875b12256dec48bc03d4c4104` |
| `.tmp/pack-smoke/results.json` (structured result) | `72f11ce947897534c9c9944b855ee1a8b08663464ca5ae3bc0f1179fe47cb7aa` |
| `scripts/pack-smoke.ts` (tested full file bytes, before main’s lint/restoration-helper follow-up) | `b104f3941d85c7878228d382456ba9daaad908bcaa56fe2aea6013b7e7f2b974` |
| Tested pre-follow-up output bytes of `git diff -- scripts/pack-smoke.ts` | `9539140a298852b31de79593ea87b42930a36e4b96f47f9e91bfaf79ba28d251` |
| Tested `openmirai-intl-compiler-0.3.29.tgz` | `2a7aef04acc6c0b2beceec8310eb696e10758f9ba75c42dd4b4342738f9719d3` |

The structured result also binds the other four library tarballs and the smoke catalog tarball. Main is snapshotting the result; `.tmp/pack-smoke/results.json` is overwritable by later runs, so retain the recorded bytes/hash with that snapshot.

Executed from L, with network permission for the isolated consumer dependency install:

```sh
PATH=/Users/kentakoong/.nvm/versions/node/v24.18.0/bin:$PATH MIRAI_INTL_ENGINE=rust pnpm_config_verify_deps_before_run=error pnpm_config_enable_global_virtual_store=false node --import tsx scripts/pack-smoke.ts > /private/tmp/intl-pack-smoke-final-green.log 2>&1
```

An initial run stopped before tests on sandbox DNS (`/private/tmp/intl-pack-smoke-final.log`). The first network-enabled run exposed an overly generic verifier diagnostic expectation (`/private/tmp/intl-pack-smoke-final-network.log`): extra-key verification correctly returned the precise locale-key error. The final run keeps the exact `generate` predicate diagnostic and permits the verifier either that same diagnostic or stale/corrupt authority. Nonzero status, no process error/signal, unchanged generated inventory/bytes and pointer remain mandatory. This is a test-expectation correction, not a relaxed compiler predicate.

| Executed case / source | Exact observation |
| --- | --- |
| `scripts/pack-smoke.ts:847` seven-case loop | `missing-nested-en`, `missing-nested-th`, `missing-nested-key`, `extra-nested-key`, `empty-translation`, `invalid-icu`, `incompatible-contract` all passed. EN/TH files are under `src/locales/receipt/`; key faults target `receipt.nested.detail`. Except for the deliberately removed file/key or added key, complete baseline objects remain intact. |
| `scripts/pack-smoke.ts:900` failure/restoration transaction | Each case asserts its intended generation diagnostic, rejected old authority, unchanged generated-byte identity and `current.json`, restores exact original locale bytes in finally, successfully proves/verifies baseline before proceeding, and checks baseline generated bytes match. |
| `scripts/pack-smoke.ts:985`–`:1050` valid change | Intact selected output exists before modifying both locale leaves from `{name}` to `{count, number}`. Old authority rejects both before and after generation; generated identity changes; fresh proof verifies and differs from old authority; installed runtime renders `Updated 7 items` / `ปรับปรุง 7 รายการ`. An unchanged generation rerun stays byte-identical and verifies. Finally restores and proves baseline, including `Hello Mali` / `สวัสดี Mali` runtime output. |
| `scripts/pack-smoke.ts:1090` retained eleven-case receipt matrix | `v1`, `malformed-v3`, `noncanonical-v3`, `tampered-v3`, `stale-source`, `stale-config`, `stale-provider`, `stale-generation-receipt`, `stale-payload`, `stale-control`, `stale-typescript-lib` all passed. These eleven are not eleven source/config cases: they include receipt/schema and generated-artifact faults. |
| `scripts/pack-smoke.ts:38`, `:1199`, `:1263` mutation safety | Provider mutation uses a fresh file plus atomic rename, not in-place truncation of a potentially store-hardlinked inode. TypeScript-lib tamper renames the original aside, writes a new file and restores the original in finally. |

The runtime check executes emitted precompiled descriptors with the installed runtime/backend. As in the existing pack smoke, it materializes the compact carrier outside the authorized generated directory because stock Node does not implement the framework private-carrier loader. This is real packed runtime execution, not a Turbo framework production build. Version-only packed generation/fresh-authorization checks and NodeNext typecheck also passed in this run.

### Subsequent restoration-helper review

Main subsequently fixed scoped style diagnostics and replaced the locale `finally` bodies with `withRestoredPackedLocales(operation, verifyRestoration)` (`scripts/pack-smoke.ts:840`). Inspection found no actionable test-logic regression: restoration is attempted after either operation success or failure; successful restoration must restore the locale files, prove/verify baseline and run the generated/runtime assertions; original operation and restoration errors are both retained in `AggregateError`. A restoration-stage error aborts the smoke rather than allowing the next case or recording success. All seven cases, exact generation diagnostics, and positive-change assertions remain present. This was inspection only, not a helper fault-injection test.

The earlier successful K hashes above remain bound to the pre-follow-up script. Main's final forced-Rust rerun after restoration error aggregation also passed (exit0). `expanded-packed-strictness.json` records the final command, full result and exact log/result hashes. Scoped lint passed. These results remain local `.29` packed evidence; post-bump CI gates are separate.

## Translation structure and source semantics

| Guarantee | Actual test name and body evidence | Source barrier | Scope / outstanding packed or Turbo proof |
| --- | --- | --- | --- |
| Required EN/TH files, including nested locale directory | `test/convention.test.ts:404` “uses workspace required locales to pinpoint a missing locale file”; `:1222` “points required-locale omissions to the locale file that must be created” creates only `src/locales/global/en.json`, requires EN+TH and asserts exact missing TH filename/locale. `test/cli.test.ts:730` “points missing locale files to the exact file that must be created” checks CLI diagnostic. | `src/catalog.ts:726` required-locale omission; `:3084` checks each directory against required locale baseline. | C/N plus K: symmetric nested missing-EN and missing-TH packed CLI negatives now executed. Actual Turbo-tree negatives remain separate. |
| Missing/extra keys at a message path | `test/catalog-strictness.test.ts:65` “rejects a missing required translation key” asserts locale/file/path as well as diagnostic. `test/strict-receipt-verification.test.ts:305` table “rejects %s without repairing or authorizing it” includes missing and extra translation key; preserves changed bytes and old pointer. | `src/catalog.ts:1246` recursive locale-key equality; `src/check-receipt.ts:1351` fresh final verification. | K adds executed isolated missing/extra nested-key packed negatives with intended diagnostics and unchanged generated state. This is not an exhaustive depth sweep. |
| Empty/whitespace, wrong kind, null | `test/catalog-strictness.test.ts:30` “rejects a %s required translation” cases empty, whitespace-only, null, wrong-kind: existing `greeting` key is preserved. The second table “rejects uniformly invalid %s message values” covers both locales null/number. | `src/catalog.ts:1171` cross-locale kind equality; `:1185` non-empty translation string. | Isolated predicate tests; C/N plus K empty-string packed negative. Whitespace/null/wrong-kind remain source-unit coverage. No meaning, grammar, translation accuracy or linguistic-quality claim. |
| Exact locale set | `test/compiler.test.ts:491` “rejects incomplete or extra locale sets” removes TH or adds FR to the same message; `test/convention.test.ts:935` “fails closed for duplicate mounts, mixed locale sets, and exact or prefix message collisions”. | `src/compile.ts:86`; mounted convention composition validation. | Direct compiler/convention tests, not proof every Turbo mount has a tested negative. |
| Malformed ICU must not become accepted cached IR | `test/shared-message-semantics.test.ts:87` “keeps raw styles distinct and never caches errors or freezes caller schemas”; `test/workspace-shared-semantics.test.ts:248` “rereads changed shared bytes after preload and never accepts a now-invalid translation”. `test/strict-receipt-verification.test.ts:305` includes invalid ICU. | `src/parser.ts:434` memo wrapper calls uncached parser for exact new input; `src/message-semantics.ts:181` memo computes before retaining success; child raw catalog reads remain. | K now executes the isolated malformed-ICU diagnostic against installed CLI with other keys preserved; direct warm-cache and real-child negatives remain separate. Historical masked parity results are not promoted. |
| Arguments, formatter roles/versions, rich tags, structured values | `test/compiler.test.ts:515` “rejects locale argument-name signature drift”; `:531` “rejects formatter syntax that disagrees with the declared argument role”; `:592` “requires every message formatter to have an exact manifest version entry”; `:703` legal rich-tag reordering/nesting vs drift; `:722` exact structured result shape. `test/parser-inference.test.ts:102` table “rejects locale contract drift” varies role, tag/multiplicity, exact branches, raw number style. | `src/parser.ts:414` parsed-contract comparisons; `src/compile.ts:112`, `:120`, `:127` signature/exact-branch/tag checks. | C/N, targeted failures keep the other message context intact. P gives successful production consumption, not packed negative coverage of every contract. |
| Locale-specific plural rules remain legal | `test/compiler.test.ts:657` “allows locale-specific CLDR categories but rejects invalid Thai categories” permits English one/other vs Thai other and rejects Thai one. `:687` “rejects locale drift in exact plural branches”. | Parser locale-sensitive plural validation plus `src/compile.ts:120`. | Structural/CLDR contract only; no linguistic-quality claim. |
| Unknown translation path cannot authorize | `test/cli.test.ts:1561` “fails check when source analysis finds unknown translation keys”: injects `t("missing")`, requires nonzero exit and `INTL_SOURCE_INVALID`, `valid:false`, one semantic file. | Source analysis/transform path lookup before proof publication; `src/proof.ts:1955` onward final integrity barriers do not replace initial semantic checks. | Actual CLI fixture negative in C/N; not claimed as a Turbo production-tree unknown-key injection. |
| Dynamic use is finite/proven, not blanket-banned | `test/transform.test.ts:738` “requires a parsed boundary key to be narrowed before translation”; `:982` “lowers finite named-key unions into one minimal deterministic full-path registry”; `:1122` rejects widened mapped property keys; `:1181` preserves finite mapped keys in a compatible shared Program. `test/cli.test.ts:1601` rejects escaped translator binding and `:1627` unbound translator props. | `src/transform.ts` finite provider closure and symbol-aware lowering (e.g. `:1517`, `:1730`); semantic authorization runs for unresolved candidates. | Unknown lexical classification is not automatically invalid translation use. Finite/narrowed dynamic keys are intentionally accepted. |
| Hardcoded visible prose is diagnosed without overclaiming all strings | `test/analyze-hardcoded-literals.test.ts:6` “flags JSX prose and user-facing props”; `:24` “flags Zod prose messages”; `:39` “does not allow comment-based escapes for production prose”; `:58` does not misclassify String.prototype helpers as Zod. Bodies assert specific messages/absence. | `src/analyze-hardcoded-literals.ts`, including Zod diagnostic at `:199`, invoked by source analysis. | Heuristic supported surfaces, not a universal proof of no untranslated UI string. C/N unit evidence; no packed all-UI prose audit asserted. |

## Authorization closure, mutation barriers, and safe recovery

| Guarantee | Actual test name and body evidence | Source barrier | Scope / limitation |
| --- | --- | --- | --- |
| Changed source and new/deleted/renamed files invalidate authority | `test/strict-receipt-verification.test.ts:100` “rejects source edits interleaved after the initial source-hash pass”; `:351` omitted source. `test/check-receipt-v2.test.ts:1939` “rejects post-authorization source %s without semantic verification” covers addition/deletion/rename while TypeScript import is mocked to throw. `test/authorization-integrity-mutation.test.ts:262` “rejects a mid-analysis source %s” covers the same before publication. | `src/check-receipt.ts:1247`, `:1284`, `:1351`; `src/proof.ts:1988`, `:2052`. | Tests establish exact source inventory/fresh hashes; verification is not a second semantic audit. C/N. |
| Declaration shadowing and owner coverage cannot shrink scan silently | `test/check-receipt-v2.test.ts:1981` “rejects a %s implementation added beside an authorized declaration” covers ts/mts/cts; `test/proof-source-universe.test.ts:54` rejects eligible sources outside every owner project; `:72` includes owner-tsconfig workspace siblings. | Check-project source-universe reconstruction at `src/check-receipt.ts:1247` and `:1683`. | C/N. Owner/checker project counters are configuration/evidence counts, not actual TS Program constructor counts. |
| Provider negative frontier is live | `test/check-receipt-v2.test.ts:1666` “rejects provider authority after %s”: nearer package, implementation, module-suffixed file, removal, exports/types retarget, package version, transitive exports. `:1791` hoisted resolution change. `test/snapshot-reuse.test.ts:418` “rejects a provider frontier mutation after the memoized capture pass”; `:473` package-control mutation. | Resolution-control/frontier verification in check-receipt; `src/proof.ts:2063` current provider input comparison. | Existing V2 negatives remain relevant to shared verification machinery but are not silently relabelled as independent V3/packed tests for every case. |
| Config and transitive config reads preserve operational errors | `test/convention.test.ts:274` identity-binds confined transitive configs; `:306` rejects escaping/symlink configs. `test/snapshot-reuse.test.ts:602` check-project mutation. `test/authority-bundle.test.ts:282` “does not classify %s filesystem failure as a recoverable cache miss”, config case extends a distinct base and fails its second lstat with EACCES. | `src/catalog.ts:2565`, `:2603` only ENOENT is absent; `src/proof.ts:2057` final config comparison. | Deterministic injected lstat boundary is meaningful; an earlier generic fault position that passed before the fix was not valid red evidence. `catalog-regression-red-green.txt` records prior proof separately. |
| Compiler/dependencies, including non-entry files, are bound | `test/authorization-integrity-mutation.test.ts:149` “rejects a mid-analysis %s mutation” covers compiler proof.ts, TypeScript _tsserver.js, lib.es5.d.ts, parser manipulator.js; `:237` delayed compiler mutation. `test/integrity-identity.test.ts:64` portable shipped module hash; `:107` metadata/resolved bytes; `:176` declaration mutation. | `src/proof.ts:1964`, `:2047`, `:2105`; immutable identity manifest and final fresh recomputation. | C/N. Cached initial setup never establishes validity after final live mutation. |
| Directory ledger catches non-source topology changes | `test/snapshot-reuse.test.ts:551` “rejects a non-source directory-ledger %s and writes no receipt”: insertion, deletion, case-only rename, symlink replacement of marker. | `src/proof.ts:2086` filesystem transaction ledger. | C/N fixture evidence, including changes not counted as semantic TS files. |
| Generated payload, selectors and publication state cannot be silently repaired by verify | `test/strict-receipt-verification.test.ts:109` payload changed after initial validation; `:357` extra builds remain untouched. `test/check-receipt-v2.test.ts:697` “never falls back after selector or selected-artifact corruption”; `:1039`, `:1067` failed selector/authority inspection cannot fall back to valid V2. | Selected immutable authority loading; final payload check `src/check-receipt.ts:1351`; proof generated comparisons `src/proof.ts:2071`, `:2091`. | Explicit check receipts V2/V3 coexist; check receipt V1 is deliberately rejected (`check-receipt-v2.test.ts:1916`), distinct from generation receipt V1. |
| Malformed/missing archive is only a candidate miss, never new authority | `test/authority-bundle.test.ts:206` “returns a safely cleaned reuse miss for a %s candidate” covers missing/malformed/stale, no imported selector, no transfer debris, then independent prove succeeds for valid inputs. `:247` “a rejected cache cannot turn invalid current translations into authority”. | `src/authority-bundle.ts:96` candidate-only failure classifier; `:729` miss result; `:813` cleanup prerequisite. | C/N; T1 demonstrates positive hit/import, not all corrupted archive negatives in Turbo. |
| Archive inventory, confinement and receiving catalog closure | `test/authority-bundle.test.ts:478` “rejects %s archive members before import” missing/changed/extra; `:517` symlink rejected before writing; `:420` omitted catalog; `:431` root locales plus src/locales; `:552` stale receiving workspace authority; `:595` “validates workspace authority references: %s” checks generation/source/content refs. | Archive bounds/safePath and inventory validation in `src/authority-bundle.ts:38`, `:124`; transferred receipt batch validates live checkout. | C/N. Positive clean relocated transfer at `:173` plus CLI export/import at `:679`. |
| Permission/resource/cleanup failure must not become safe fallback | `test/authority-bundle.test.ts:282` archive/source/config/cleanup EACCES all reject with code. `test/verification-reads.test.ts:60` “retains stale and filesystem failures instead of downgrading the latter”; `:78` drains mixed nested failures. | `src/authority-bundle.ts:109` preserves non-candidate codes and recursively causes/AggregateError; `src/check-receipt.ts:1575` keeps prior operational failure with final barrier failure. | C/N injected faults. A successful reuse miss is not an excuse to ignore EACCES/cleanup errors. |
| Rollback must finish before recovery is safe | `test/authority-bundle.test.ts:329` “permits recovery only after successful rollback (failed rollback=%s)” mutates source after install. Success restores prior generated pointer and authority selector bytes; failure retains transfer lock and backup directory and rejects. `:265` existing transfer lock/publication recovery forbids missing-candidate fallback. | `src/authority-bundle.ts:669` rollback failure becomes recovery error; `:813` failed rollback/cleanup never permits fresh audit. | C/N. Packed/Turbo fault injection of rollback failure remains a gap; T1 only validates the success route. |
| Interrupted generation is handled by writer transaction, not loose salvage | `test/generation-receipt-fast-path.test.ts:232` “delegates an exact interrupted %s journal to writer recovery” covers PREPARED through VALIDATED; `:263` malformed journal hard-fails; `:395` partially missing selected payload hard-fails; `:895` corrupt output does not regenerate over changed inputs. | Generation receipt validation delegates exact journal recovery to writer; input barriers before/after pointer commit. | C/N. Whole missing payload recovery is intentionally distinct from partial corrupt output. |
| Full app version and raw manifest/lock are rechecked at final return | `test/strict-receipt-verification.test.ts:368` “rejects a version-only mutation after final classifier checks (%s)” runs standalone, batch, transfer. `:417` “rejects raw manifest formatting changes after final classifier checks”; `:439` “rereads raw lock bytes after the final full application identity capture”. Hooks assert mutation actually occurred. | `src/check-receipt.ts:1380` freshly computes full application identity, `:1386` repeats raw manifest/lock binding; deferred final checks at `:1545`. | Three-route version regression is explicit. Raw-format/lock negatives are standalone tests, not independently repeated for all three routes. |

## Reuse equivalence, native operation, and shared compilation

| Guarantee | Actual test name and assertion | Source barrier | Scope / limitation |
| --- | --- | --- | --- |
| Version-only generation bytes stable; old authority rejected | `test/generation-receipt-fast-path.test.ts:156` “preserves every generated byte across an application version-only bump” recursively snapshots output, requires unchanged result and zero compile/emit. `test/snapshot-reuse.test.ts:274` “requires fresh authorization after a version-only bump without rewriting output” rejects prior authority, performs one fresh compile and source analysis, changes receipt, keeps output controls/content hash, verifies fresh receipt, rejects another bump. | `src/catalog.ts:3119` convention content-v1; `:3419` declared generation projection; full authorization remains at check-receipt final barrier. | App version only, not compiler version. Do not claim fresh authorization performs zero compilation. Legacy projection/compiler changes can require one clean regeneration. K also passes packed version-only stability/fresh-authorization checks; Turbo version probe still needs completed CI evidence. |
| Explicit public buildId behavior retained | `test/compiler.test.ts:277` “retains explicit buildId and descriptor tokens for direct compiler callers”; `test/integrity-identity.test.ts:250` “excludes only the top-level version and keeps full authorization provenance”. | `src/compile.ts:442` uses caller buildId; convention alone sets content-v1. | Direct API test; no broadened omission of dependencies, nested version values, or lock bytes. |
| Unchanged reruns avoid redundant work and preserve output | `test/compiler.test.ts:1212` byte-identical clean generations; `:1254` unchanged artifact set not rewritten; `:1280` stale destination not called unchanged. `test/generation-receipt-fast-path.test.ts:133` valid unchanged receipt zero compile/emit; `test/cli.test.ts:1253` “uses the receipt-first unchanged ensure path without Program, compile, or emit work”. | Receipt-first validation plus current inputs/payload checks, not an unconditional cache hit. | C/N; P verifies builds with zero semantic builds; T1 positive reuse. No universal timing claim. |
| Real translation edit invalidates generation and rebuilds | `test/generation-receipt-fast-path.test.ts:421` “reconstructs a completely missing selected payload after changed inputs” preserves all existing EN keys with spread, changes appName, requires changed:true and a different selected directory. `test/cli.test.ts:1221` “ensures missing and stale catalogs while leaving a current catalog unchanged”. | Raw locale inputs bound to generation identity; actual message bytes enter `src/compile.ts:388` locale hashes. | K closes the packed intact-output valid leaf/contract change → stale auth rejection → generation change → fresh proof → EN/TH runtime change → unchanged rerun guarantee. The actual Turbo-tree production scenario remains separate. |
| Weak canonical cache cannot trust arbitrary caller objects | `test/receipt-canonical-cache.test.ts:451` one canonicalization of trusted deep-frozen graph; `:516` arbitrary mutable/caller-frozen forged hash rejected; `:534` full untrusted rebuild preserves named hashes/counters. | `src/authorization-snapshot.ts:4694` trusted WeakSet; `:7748` trusted structure shortcut still `validateV3Relationships`; `:7858` canonical bytes. | Internal trusted structure only; weak identity cache is not arbitrary IR authorization. |
| Operation receipt cache retains fresh source checks | `test/receipt-canonical-cache.test.ts:474`, `:502`, `:608` exact test names describe retained identity/fresh evidence; bodies alter source slice hash and inject EACCES, require rejection, then verify new operation performs native validation again. | `src/authorization-snapshot.ts:7887` async parser, `:7933` parsing scope, `:4296` live relationships; `src/check-receipt.ts:1351` final current inputs. | Unit native parser return is mocked in these cache tests. Real native equivalence requires separate gates. |
| Native canonical fast path preserves schema reconstruction | `test/receipt-canonical-cache.test.ts:543` “native byte validation retains named hashes, counters and live source evidence”: mocked native true still rejects altered named hash/counter and changed source. `test/authorization-snapshot-v3.test.ts:2263` malformed fields/refs/order/hashes; `:2298` every named V3 hash. | Async parser validates byte form then reconstructs schema and validates V3 relationships; native canonical success is not a receipt signature. | C/N for integration; `native/test/canonical-receipt.test.mjs:59` addon/worker “streaming receipt decisions match Node over deterministic numeric/Unicode mutations” is a separate node:test suite, not automatically in pnpm verify. |
| Unsupported native input can fall back, failures cannot | `test/receipt-canonical-cache.test.ts:583` “native unsupported input uses the full Node canonical validator”: undefined uses Node, noncanonical rejected; false rejects; EACCES rejection propagates. | `src/native-engine.ts:166` bounded request path distinguishes unsupported from operational errors. | C/N unit branch coverage; real input corpus evidence must be attributed to its own native test run. |
| Native absent vs malformed assets | `test/native-assets.test.ts:113` “permits Node fallback only when the entire native directory is absent”; `:61` same-length binary mutation; `:69` missing/extra/symlink; `:89` version/ABI/target/size rejection. `:44` portable manifest checks selected executable. | `src/native-assets.ts:122` ENOENT-only absence; `:146` exact compiler version and manifest validation. | Normal clean checkout absence permits auto Node; stale ignored `.29` assets beside bumped manifests fail closed. Unsupported host selection differs from malformed package. These tests use fixtures, not eight real loaded operating systems. |
| Verified bytes loaded despite package replacement | `test/native-loader.test.ts:96` “loads owned verified bytes even when package bytes are swapped and restored at dlopen”; `:118` persistent replacement poisons retries; `:138` dlopen EACCES does not retry and snapshot removed; `:186` snapshot corruption rejected before dlopen. | `src/native-engine.ts:35` serialized initialization/poison; `:108` private snapshot; `:142` post-load package recheck. | Deterministic mocked loader tests plus real native integration gates. Same-uid hostile mutation inside private temp is outside declared trust boundary. |
| Bounded native initialization, lifecycle and queue | `test/native-loader.test.ts:158` twelve concurrent initializations and later operation yield one snapshot/load; Windows locked snapshot test at `:238`; post-load cleanup failure at `:202` is skipped on Windows. `native/test/integration.test.mjs:141` 16 concurrent requests across four operation scopes; `:165` rejects close while active and execute after close. | `src/native-engine.ts:183` pending 64 limit; `:194` queued-byte bound; `:242` drain/close; `:257` ALS scope; CLI `src/cli.ts:1470` wraps execution. | Separate native node:test execution not inferred from N. New native-loader.test.ts:287 saturation cases now reject request65 and excess aggregate bytes at64MiB, drain accepted work serially, retain EACCES, recover counters and close after completion. All10loader tests pass (fixture addon; this proves Node bridge admission/lifecycle, not a real-addon saturation load test). Windows cleanup is simulated in unit test; eight-target runtime smoke is a different evidence layer. |
| Read dedup is phase-scoped and drains failures | `test/verification-reads.test.ts:7` “shares the observed hash, never an expected-hash verdict, within one phase”; `:25` bounded active reads and drain; `test/transferred-verification-batch.test.ts:30` “shares compiler identity only between the initial and final transfer barriers” counts exactly two immutable captures for two catalogs then rejects source edit. `test/strict-receipt-verification.test.ts:245` changes a shared source between catalog finals and later catalog rejects. | Fresh final checks `src/check-receipt.ts:1379`, `:1545`; no cross-final observed-hash cache. | C/N, bounded fixture counts, not actual TS Program counts. |
| Shared semantics preserve per-app identity and raw-input validation | `test/shared-message-semantics.test.ts:51` “reuses compiled templates while remounting identities and emits every representation byte-for-byte”; `:175` changed formatter/locale/tags/kind/unsafe app keys rejected; `:255` remount rebuilds validator slots/provenance; `:305` full schema+locale key and owned copies. | `src/message-semantics.ts:181` exact scoped memo; `:243` no ambient session fallback; compile wrapper reconstructs app identity. | C/N and S. This reuses semantic templates, not a shared app receipt, generated descriptor identity, or an npm runtime dist bundle. |
| Compile once crosses actual child processes | `test/workspace-shared-semantics.test.ts:171` “preloads once and reuses exact semantics in five real child processes with byte parity”; importantly `:333` “counts actual workspace coordinator ICU work separately from all five prove children” launches actual CLI check --workspace and records parent parseCalls=2, five prove children each 0. | `src/cli.ts:461` parent session/preload, `:480` immutable identity recheck, `:484` serialize; child `:1456` private receive; all per-child authorization retained. | The 2/0 fixture is distinct from S’s real packed 5,718 shared raw strings. Neither means every app-specific translation compiles once across unrelated processes. |
| Private channel bounds/context are not public IR authority | `test/message-semantics-channel.test.ts:37` wrong session/truncation/trailing/tamper; `:67` context mismatch with valid digest; `:88` oversized declaration; `:147` changed compiler bytes at same version. `test/workspace-shared-semantics.test.ts:237` rejects public snapshot args; `:287` real stalled inherited pipe exits; `:410` mismatched compiler capsule rejected. | `src/message-semantics-channel.ts:18` declared trusted coordinator boundary; `:22` size bound; `:126` context including immutable identity; `src/message-semantics.ts:258` shape/bounds/identity validation. | Private pipe trusts the executing compiler parent. PID/env/nonce/hash are session/framing checks, not hostile-parent authentication. No public snapshot import/file cache claim. |

## Counter semantics and historical equivalence caveat

The original authority receipt semantic count is **96 + 5 + 44 + 24 + 3 = 172**, while the lexical/source inventory is **5,117**. `unknownActiveSources=1` and `unknownBoundaryIdentities=1` for shared can coexist with success: these are evidence/classification counters, not one invalid translation. `test/analyze-sources-candidate.test.ts:44` “sends only proven-present and unknown-active sources to semantic Programs” explicitly distinguishes candidate routing from validity. `test/authorization-snapshot-v3.test.ts:2808` checks multibyte unknown-boundary evidence, and `:2834` does not count mixed literal-present sources as unknown-active. One owner project / zero checker projects and large loadedLibFiles counts (e.g. admin 8,544) must not be labelled TS Program counts. Actual constructor work needs instrumentation, as in `phase0-reference-parity.test.ts:712` “creates one shared Program per exact owner and none for a checker”.

Historical `receipt-parity.ts` cases replaced a whole 1,000-key locale with a one-key object. Their “invalid ICU” and “empty translation” cases therefore failed first on missing `message1`, masking the intended predicates. This was **not cross-case contamination**: files were restored. Those JSON results prove rejection only, not isolated ICU/empty predicate parity. K now closes these isolated packed predicate gaps; future actual-Turbo fault cases must copy the original object, modify exactly one leaf, preserve all other keys/locales/contracts unless that is the single tested fault, assert the intended diagnostic and unchanged authority/pointer, restore the case, and independently prove the baseline valid again.

## Remaining approval gaps

1. Await T3 completion; T1/T2 typecheck failures are not adoption success, and T3 source-quality success/tests-running is not full candidate success. Archive the fresh post-build finalized proofs/finalize JSON before version-probe or reuse mutations, along with exact candidate pin/artifact/source/tree IDs.
2. P has honest hashed logs but lacks preserved finalized proof objects; do not reconstruct a claim that those exact local proof files survived. CI archival must close this gap.
3. K closes seven isolated packed locale negatives and the intact-output valid translation/contract-change runtime path, while retaining the eleven receipt negatives including source/config/provider/TypeScript-lib tamper. It does not execute all provider-negative-frontier/dependency permutations or unsafe rollback/permission faults against packed consumers. Nor does the isolated fixture establish the actual Turbo-tree strictness probe; main owns that separate acceptance evidence.
4. Separate source unit tests, real native addon/worker oracle tests, eight-target runtime smoke, packed library smoke, and actual Turbo builds. `pnpm verify` does not select `native/test/*.test.mjs`; do not count those named test bodies as executed without their own logs. Node bridge queue saturation is now exercised by10passing loader tests; platform-specific loaded-DLL cleanup remains simulated where stated.
5. This matrix establishes structural/authorization guarantees and bounded equivalence evidence. It makes no linguistic-quality, production deployment, publication, or exhaustive-security claim.
