# Final acceptance gap audit — R37 / Tcb22

Read-only scope: R `37a8628f66cd793330db2d5a163287f8454182a8`; T `cb22e0506772543cf24a384b25e2850d96e2f074`. No tests, proves, builds, Git mutations or source edits executed for this audit. Requirements: T `docs/plans/intl-native-ci/agreed-plan.md:364-395` strictness matrix and acceptance checklist. Warm benchmarking excluded (separate owner).

## Evidence boundary

- Current corrected37 Linux Turbo lanes: main reports BOTH producer/export, unchanged reuse and isolated import passed; quality and repeated benchmark were still running at last update. This is stage-level evidence, not finished17-case strictness/four-app version/build acceptance. No completed current Turbo report was inspected here.
- R `.tmp/native37-jobs.json` is an older snapshot: eight native targets and assembly success, packed verification still in progress. Do not present this snapshot as a completed whole native37 run. Native workflow `.github/workflows/native-build-ci.yml:80` runs forced-Rust `pnpm verify`; the archive/report from the final job is the completion evidence needed. Normal run34494038352/native34494038965 were identified in session; current terminal conclusions were not refreshed in this audit.
- Historical `.29` Turbo34468546926 completed its then-configured gates; it predates the corrected semantic-source strictness probes. Historical R packed smoke recorded7 locale negatives,11 receipt negatives, genuine translation/runtime checks; later local full verification log also contains a packed failure, so a partial green subsection is not whole-command success.
- G R37 packed producer failed the internal20s publication window; standalone Auth repeated it. No17-case or version probes ran. Reports remain at `/private/tmp/intl-native-semantic-consumer.bPVcp5Wo/.intl-candidate/evidence/`. This proves fail-closed publication, not malformed-input acceptance or rejection. R-direct F all5/899 semantic success uses installed .29 declarations; it cannot stand in for fully packed .30 consumer proof.

## Guarantee → exact existing checks

Paths below are relative to R unless prefixed T. Named tests were inspected; presence is distinguished from execution above.

| Guarantee | Exact test / source reference and scope |
|---|---|
| EN/TH missing nested locale; complete nested keys | `scripts/pack-smoke.ts:870`: cases `missing-nested-en`, `missing-nested-th`, `missing-nested-key`, `extra-nested-key`; whole original key objects preserved and intended diagnostics asserted. T `.github/scripts/verify-intl-strictness.mjs` cases `missing-en`, `missing-th`, `missing-nested-key`, `extra-nested-key` exercise actual Auth. Current corrected Turbo result pending. |
| Empty/whitespace/wrong-kind; ICU syntax/contracts | `catalog-strictness.test.ts:30` “rejects a %s required translation” (empty, whitespace-only, null, wrong-kind), :65 “rejects a missing required translation key”; packed cases `empty-translation`, `invalid-icu`, `incompatible-contract`. These are isolated predicates, unlike the historical1000-key replacement microbench. |
| Unknown/dynamic/supported adapter usage and hardcoded UI | `classifier-semantic-fallback.test.ts` “approved filtering matches unfiltered semantics for $name” (resolved/default/server hooks, valid alias, ordinary imported t positive parity); `semantic-source-receipt.test.ts:97` “generated and proved semantic activity verifies and reuses after portable remount”; :170 kind/reason substitution; :183 “rejects a truncated whole-source observation after public rebuilding recomputes named hashes”; :200 same-length source mutation. T actual `unknown-source-key`, `dynamic-source-key`, `hardcoded-source-prose` results must come from new report, not historical producer success. |
| Fresh source set, shared source and added catalogs | `strict-receipt-verification.test.ts:100` “rejects source edits interleaved after the initial source-hash pass”; :138 new catalog; :245 “rereads a shared source when it changes between catalog final checks”; :351 omitted source. Its :309 table includes changed/new source and config, new locale/lock and unexpected generated state, asserting unchanged pointer and no repair. |
| Provider negative frontier / resolution controls | `classifier-candidate-shadow.test.ts:606` “invalidates negative topology evidence when a nearer manifest appears”; `classifier-candidate.test.ts:503` “rejects absent lockfile and nearer-manifest creation”; `snapshot-reuse.test.ts:418` provider frontier mutation and :473 provider package-control mutation; `check-receipt-v2.test.ts` table cases “nearer package addition”, “exports retarget”, “types retarget”, “transitive exports retarget”. |
| Current full manifest/lock after initial/deferred checks | `strict-receipt-verification.test.ts:367` “rejects a version-only mutation after final classifier checks (%s)” for standalone/batch/transfer; :417 raw manifest formatting; :439 raw lock bytes after final full application capture. These protect full authorization independently of generation's version projection. |
| Version-only stable outputs, fresh authority; explicit API | `generation-receipt-fast-path.test.ts:156` “preserves every generated byte across an application version-only bump”; `snapshot-reuse.test.ts:274` “requires fresh authorization after a version-only bump without rewriting output”; `compiler.test.ts:277` “retains explicit buildId and descriptor tokens for direct compiler callers”. T `verify-intl-generation-identity.mjs` loops all4 apps, tests old rejection/fresh prove/stable rerun, restores manifest bytes but leaves probe authority stale; later consumers need restored-baseline prove. |
| Real translation changes generation and remains renderable | `scripts/pack-smoke.ts:1085` translationChangeEvidence asserts changed generation, old rejection, fresh authority, actual runtime EN/TH and unchanged rerun; T strictness `valid-translation-change` additionally restores and proves baseline. Packed runtime evidence is not linguistic-quality evidence. |
| Legacy selectors/receipt schemas and no corrupt fallback | `check-receipt-v2.test.ts:631` “supports legacy V2, explicit V2 with dormant V3, and activated V3”; :697 “never falls back after selector or selected-artifact corruption”; :1039 selector inspection failure; :1067 selected-authority inspection failure; :1344 portable V3 envelope missing/extra/swapped/corrupt owners. |
| Generated corruption versus safe reconstruction | `generation-receipt-fast-path.test.ts:285` missing receipt only from independently matching state; :373 completely missing selected payload; :395 partially missing payload hard failure; :263 malformed journal; :565 restores previous generation after late mutation; :895 “hard-fails corrupt output instead of regenerating changed inputs”. `strict-receipt-verification.test.ts:109` late payload mutation and :357 extra builds retained/rejected. |
| Safe cache misses, unsafe publication/permissions/rollback | `authority-bundle.test.ts:206` “returns a safely cleaned reuse miss for a %s candidate” (missing/malformed/stale) proves independent fresh authorization; :247 invalid current translations remain invalid; :281 archive/source/transitive-config/cleanup EACCES is not a miss; :328 “permits recovery only after successful rollback (failed rollback=%s)”; :455 prior state preserved. T `prepare-intl-authority.test.mjs:109` one audit on structured safe miss, :123 operational/unsafe misses never authorize/export, :206 failed check/drift/export fatal. |
| Archive confinement, complete membership and no salvage | `authority-bundle.test.ts:420` omitted catalog, :477 missing/changed/extra selected content, :517 symlink archive members, :527 export destination never overwritten, :552 receiving authority absent from bundle, :577 bounded tar overhead. Native transfer remains the trust boundary after download provenance checks. |
| Canonical Node/Rust numeric/Unicode bytes | `native/test/engine.test.mjs:19` addon AND worker “canonical JSON matches the existing Node encoder”: negative zero/exponent extremes, Thai/astral/combining strings, BMP/astral and integer-key ordering; explicit unsupported malformed/overflow/surrogate cases. `native/test/canonical-receipt.test.mjs:59` addon AND worker “streaming receipt decisions match Node over deterministic numeric/Unicode mutations”: seeded corpus, whitespace/newline mutations, exact hash; lone-surrogate and deep nesting unsupported. `native/src/canonical_receipt.rs:180` `rejects_noncanonical_but_preserves_unsupported_encodings` adds direct Rust malformed cases. |
| Schema reconstruction and trusted cache never skip live validation | `receipt-canonical-cache.test.ts:516` “never trusts or caches arbitrary mutable or caller-frozen objects”; :534 named hashes/counters; :543 native byte validation rejects changed live source, forged sourceAuthorizationHash and counters; :608 operation-scoped structure reuse. Native success is mocked in :543, so it isolates JS schema validation, not an independent native integration run. Native byte oracle plus forced-Rust full suite provide complementary evidence. |
| Native unsupported fallback versus operational failure | `receipt-canonical-cache.test.ts:583` “native unsupported input uses the full Node canonical validator”: undefined -> full Node; false -> rejection; EACCES rethrown unchanged. `native-assets.test.ts:113` fallback only when native directory wholly absent; :69 missing/extra/symlink assets rejected; :89 incompatible manifest/version/ABI/target/size. |
| Immutable native/compiler identity and loading races | `native-assets.test.ts:44` portable manifest, :61 changed same-length binary; `native-loader.test.ts:96` swap/restore at dlopen still loads owned verified bytes; :118 persistent replacement permanently rejects; :138 dlopen failure poisons; :158 one initialization; :186 snapshot corruption; :238 one Windows locked snapshot and exit cleanup. `message-semantics-channel.test.ts:147` changed compiler bytes despite same versions/digest; workspace shared test:410 changed capsule compiler identity. |
| Queue/concurrency lifecycle and zero-work verification | `native-loader.test.ts:287` “rejects excess pending %s, drains accepted work and preserves operational failures”: count64/65th and aggregate64MiB; blocked execution, drain and EACCES. `native/test/integration.test.mjs:141` serialized requests/reopening; :165 close/active-work rejection. `strict-receipt-verification.test.ts:194` current V3 no compilation/emission; :215 bounded workspace session/stale member. Actual current Turbo import counters cover all5 only once final report is retained. |
| Shared semantics and app-specific composition | `workspace-shared-semantics.test.ts:171` five real children byte parity; :333 actual coordinator versus five prove-child ICU counters; :248 changed raw shared bytes rejected; :237 public snapshot args rejected; :287 real inherited-pipe timeout. Compiler `compiler.test.ts` collision assertion near:773 and `shared-message-semantics.test.ts` remount cases retain app identities. IPC trusts executing compiler parent; nonce/PID/digest is not hostile-parent authentication. |
| Publication under bounded operational resources | `dormant-v3-publication.test.ts:167` “leaves the prior selector valid across every pre-commit interruption and corrupt-object failure”, including exact-deadline expiry :199-207. G adds actual failure evidence, not robustness success. See separate `.tmp/dormant-publication-deadline-diagnosis.md`; no deadline extension recommended. |

## Actual remaining acceptance gaps / disposition

1. **Await corrected37/Tcb22 complete artifacts:** all17 intended Auth predicates (especially unknown/dynamic), all4 version-only results, and final production build proofs. Current successful producer/reuse/import is insufficient. Historical .29 completed CI and local packed results must remain separately labelled. This is an evidence gate, not a new source defect.
2. **Node/Rust differential scope:** actual native oracle covers canonical bytes/numeric/Unicode and filesystem operations; JS tests cover schema relationships and mutation barriers, including mocked native outcomes. Do not call this an exhaustive paired malformed-V3 receipt oracle. If claiming that stronger guarantee, feed the same concrete malformed V3 fixtures through Node and real addon parser and compare acceptance/diagnostics. Existing complementary tests do not demonstrate such a complete paired corpus.
3. **Operational deadline diagnosis remains incomplete:** no failure-phase timing proves whether staging, fresh verification or scheduling consumed the20s. Linux success does not establish localhost cause. Keep fail-closed deadline and all source/identity barriers; no localhost performance or robustness conclusion from G.
4. **Exact baseline/candidate semantic differential is limited:** approved-vs-safe-unfiltered R tests and actual corrected consumer probes are strong current-validator evidence, not a full published.29-vs.30 identical-input diagnostic comparison. Historical masked ICU/empty microbench cannot fill this. Do not promise universal diagnostic identity outside supported forms (ordinary imported t is intentionally positive parity).

No additional concrete API-compatibility blocker identified by this bounded inspection. Do not mark the acceptance checklist complete until pending artifacts establish their exact scope. No linguistic-quality, publication, merge or deployment approval implied.
