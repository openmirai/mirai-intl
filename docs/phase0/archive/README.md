# Phase 0 archive

These files preserve the renderer-selection and pre-migration evidence accepted
before the production string-key contract in ADR 0002. They are immutable
historical records, not generated inputs and not release gates.

Current verification is defined by the workspace `verify` script and the
production compiler/runtime/application tests.
