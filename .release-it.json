{
  "git": {
    "addUntrackedFiles": true,
    "commitMessage": "chore: release v${version}",
    "tagName": "v${version}",
    "tagAnnotation": "Release v${version}",
    "push": true,
    "requireCleanWorkingDir": true
  },
  "github": {
    "release": false
  },
  "npm": {
    "publish": false,
    "versionArgs": ["--allow-same-version"]
  },
  "hooks": {
    "before:init": "corepack pnpm run verify",
    "after:bump": "node scripts/sync-workspace-versions.mjs ${version} && corepack pnpm install --no-frozen-lockfile && corepack pnpm build && corepack pnpm generate:fixtures && corepack pnpm check:generated",
    "after:release": "echo 'Release tag pushed; GitHub Actions will publish the packages to npm.'"
  },
  "plugins": {
    "@release-it/bumper": {
      "out": "packages/*/package.json"
    },
    "@release-it/conventional-changelog": {
      "preset": "angular",
      "infile": "CHANGELOG.md"
    }
  }
}
